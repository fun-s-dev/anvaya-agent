{
  "name": "@anvaya/agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'No agent lint configured yet'"
  },
  "dependencies": {
    "@anvaya/canonical": "*",
    "@anvaya/reconciliation": "*",
    "@google/generative-ai": "^0.24.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
