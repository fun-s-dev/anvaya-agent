import './globals.css';
export const metadata = { title: 'Anvaya - Reconciliation Control Room', description: 'Evidence-backed financial reconciliation' };
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return <html lang="en"><body>{children}</body></html>; }
